var v[10]: int;

def num(x: int):int {
    return x;
}

def bubblesort(v1[]: int; n: int) {
    var i=0, j, o = num(1 + 2): int;
    var trocou = true: bool;

    i = n;
    i = v1;

    i += (3 * 2) + 1;
    j = 1 + 2 + 3;

    while (j > 2) {
        trocou = false;
        for (j=0; j < 1 + (2 + 3) + (2); j+=1) {
            if (v1[j] > v[j+1]) {
                var aux = v1[j]: int;
                v1[j] = v1[j+1];
                v1[j+1] = aux;
                trocou = true;
            }

            skip;
        }
        i += 1;
    }

}

def main(): int {
    var i, j: int;

    if (1 > 2 + 3) {
        write "Foi";
    } else if (false) {
        write "Nao foi";
    } else {
        write "Piorou";
    }

    write "Digite os valores do arranjo:\n";

    for (i=0, j=1; i<10; i+=1) {
        write "A[", i, "] = ";
        read v[i];
    }

    bubblesort(v, 10);

    write "Arranjo ordenado:\nA = ";

    for (i=0; i<10; i+=1) {
        write v[i], " ";
    }

    return 3 + 4 * 2 + 2;

}